PRS 2022 final project (also referred to as "game")

needs to be installed:
python 3 or higher
nltk package, gutenberg resource(?), punkt resource(?),averaged_perceptron_tagger(?probs not)
universal_tags(?), wordnet(?), omw-1.4(?)
random package?